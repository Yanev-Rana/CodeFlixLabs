[{"code":"void trackPlayerRanks(int ranked[], int n, int player[], int m, int result[]) {\r\n    // Step 1: Create a dense rank array for the leaderboard\r\n    int denseRank[n];\r\n    denseRank[0] = 1;\r\n    for (int i = 1, rank = 1; i < n; i++) {\r\n        if (ranked[i] == ranked[i - 1]) {\r\n            denseRank[i] = rank;\r\n        } else {\r\n            denseRank[i] = ++rank;\r\n        }\r\n    }\r\n\r\n    // Step 2: For each player score, compute their rank\r\n    int i = n - 1; // Pointer to leaderboard\r\n    for (int j = 0; j < m; j++) {\r\n        int score = player[j];\r\n\r\n        // Move backward until finding the correct position\r\n        while (i >= 0 && score >= ranked[i]) {\r\n            i--;\r\n        }\r\n\r\n        // If score is higher than all ranked scores, rank is 1\r\n        if (i == -1) {\r\n            result[j] = 1;\r\n        } else {\r\n            result[j] = denseRank[i] + 1;\r\n        }\r\n    }\r\n}\r\n","language":"c","time":"2025-05-08T05:38:19.852Z","isCorrect":true}]