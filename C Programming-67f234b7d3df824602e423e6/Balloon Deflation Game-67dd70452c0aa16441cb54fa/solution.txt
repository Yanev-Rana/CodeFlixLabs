[{"code":"void deflateBalloons(int air[], int n) {\r\n    while (1) {\r\n        // Find the minimum air value greater than 0\r\n        int minAir = -1;\r\n        for (int i = 0; i < n; i++) {\r\n            if (air[i] > 0 && (minAir == -1 || air[i] < minAir)) {\r\n                minAir = air[i];\r\n            }\r\n        }\r\n\r\n        // If no balloon has air left, stop the loop\r\n        if (minAir == -1) {\r\n            break;\r\n        }\r\n\r\n        // Count how many balloons still have air before deflating\r\n        int count = 0;\r\n        for (int i = 0; i < n; i++) {\r\n            if (air[i] > 0) {\r\n                count++;\r\n            }\r\n        }\r\n\r\n        // Print number of non-flat balloons this round\r\n        printf(\"%d\\n\", count);\r\n\r\n        // Deflate all non-flat balloons by minAir\r\n        for (int i = 0; i < n; i++) {\r\n            if (air[i] > 0) {\r\n                air[i] -= minAir;\r\n            }\r\n        }\r\n    }\r\n}\r\n","language":"c","time":"2025-05-08T05:52:34.545Z","isCorrect":false},{"code":"void deflateBalloons(int air[], int n) {\r\n    int done[1000] = {0};  // track which balloons are fully deflated\r\n    while (1) {\r\n        int count = 0;\r\n\r\n        // Count all balloons that are not done\r\n        for (int i = 0; i < n; i++) {\r\n            if (!done[i]) {\r\n                count++;\r\n            }\r\n        }\r\n\r\n        if (count == 0) {\r\n            break;\r\n        }\r\n\r\n        // Print number of non-flat balloons at the beginning of the round\r\n        printf(\"%d\\n\", count);\r\n\r\n        // Find minimum air among active (non-flat) balloons\r\n        int minAir = -1;\r\n        for (int i = 0; i < n; i++) {\r\n            if (!done[i] && air[i] > 0 && (minAir == -1 || air[i] < minAir)) {\r\n                minAir = air[i];\r\n            }\r\n        }\r\n\r\n        if (minAir == -1) break;\r\n\r\n        // Deflate by minAir and mark done if air reaches 0\r\n        for (int i = 0; i < n; i++) {\r\n            if (!done[i] && air[i] > 0) {\r\n                air[i] -= minAir;\r\n                if (air[i] == 0) {\r\n                    done[i] = 1;\r\n                }\r\n            }\r\n        }\r\n    }\r\n}\r\n","language":"c","time":"2025-05-08T05:55:34.391Z","isCorrect":false},{"code":"void deflateBalloons(int air[], int n) {\r\n    int done[1000] = {0};  // track which balloons are deflated\r\n\r\n    while (1) {\r\n        int count = 0;\r\n\r\n        // Count all balloons that are not fully deflated and have air > 0\r\n        for (int i = 0; i < n; i++) {\r\n            if (!done[i] && air[i] > 0) {\r\n                count++;\r\n            }\r\n        }\r\n\r\n        // Stop if no deflatable balloons are left\r\n        if (count == 0) {\r\n            break;\r\n        }\r\n\r\n        // Count all active (not yet marked done) balloons — this is what's printed\r\n        int activeCount = 0;\r\n        for (int i = 0; i < n; i++) {\r\n            if (!done[i]) {\r\n                activeCount++;\r\n            }\r\n        }\r\n        printf(\"%d\\n\", activeCount);\r\n\r\n        // Find minimum air among non-flat balloons\r\n        int minAir = -1;\r\n        for (int i = 0; i < n; i++) {\r\n            if (!done[i] && air[i] > 0 && (minAir == -1 || air[i] < minAir)) {\r\n                minAir = air[i];\r\n            }\r\n        }\r\n\r\n        // Deflate all balloons by minAir and mark as done if they reach 0\r\n        for (int i = 0; i < n; i++) {\r\n            if (!done[i] && air[i] > 0) {\r\n                air[i] -= minAir;\r\n                if (air[i] == 0) {\r\n                    done[i] = 1;\r\n                }\r\n            }\r\n        }\r\n    }\r\n}","language":"c","time":"2025-05-08T05:57:02.589Z","isCorrect":true}]