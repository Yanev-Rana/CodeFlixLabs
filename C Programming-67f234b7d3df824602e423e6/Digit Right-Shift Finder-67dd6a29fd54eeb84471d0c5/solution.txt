[{"code":"#include <stdio.h>\r\n#include <string.h>\r\n#include <stdlib.h>\r\n\r\nint digitRightShift(int N) {\r\n    char numStr[20];\r\n    sprintf(numStr, \"%d\", N);  // Convert number to string\r\n\r\n    int len = strlen(numStr);\r\n    if (len == 1)\r\n        return N;  // Single-digit number remains unchanged\r\n\r\n    // Perform right circular shift\r\n    char shiftedStr[20];\r\n    shiftedStr[0] = numStr[len - 1];          // Last digit becomes first\r\n    strncpy(shiftedStr + 1, numStr, len - 1); // Copy rest of the digits\r\n    shiftedStr[len] = '\\0';                   // Null-terminate the string\r\n\r\n    return atoi(shiftedStr); // Convert back to integer and return\r\n}\r\n","language":"c","time":"2025-05-08T06:04:34.621Z","isCorrect":true}]