[{"code":"#include <stdio.h>\r\n\r\nint mostPlayedGame(int games[], int n) {\r\n    int freq[10001] = {0};  // Assuming game IDs are between 1 and 10000\r\n\r\n    // Count frequency of each game ID\r\n    for (int i = 0; i < n; i++) {\r\n        freq[games[i]]++;\r\n    }\r\n\r\n    int maxFreq = 0;\r\n    int result = 10001;  // A value larger than any valid game ID\r\n\r\n    // Find the most frequent game ID (and smallest in case of tie)\r\n    for (int i = 1; i <= 10000; i++) {\r\n        if (freq[i] > maxFreq || (freq[i] == maxFreq && i < result)) {\r\n            maxFreq = freq[i];\r\n            result = i;\r\n        }\r\n    }\r\n\r\n    return result;\r\n}\r\n","language":"c","time":"2025-05-08T06:16:58.649Z","isCorrect":true}]