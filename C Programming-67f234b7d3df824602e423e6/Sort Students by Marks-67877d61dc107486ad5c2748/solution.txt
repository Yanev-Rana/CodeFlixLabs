[{"code":"\r\n#include <stdio.h>\r\n#include <stdlib.h>\r\n#include <string.h>\r\n\r\ntypedef struct {\r\n    int roll_number;\r\n    char name[50];\r\n    float marks;\r\n} Student;\r\n\r\nint compareStudents(const void *a, const void *b) {\r\n    Student *studentA = (Student *)a;\r\n    Student *studentB = (Student *)b;\r\n    if (studentB->marks > studentA->marks) return 1;\r\n    else if (studentB->marks < studentA->marks) return -1;\r\n    else return 0;\r\n}\r\n\r\nint main() {\r\n    int N;\r\n    scanf(\"%d\", &N);\r\n\r\n    Student students[N];\r\n\r\n    for (int i = 0; i < N; i++) {\r\n        scanf(\"%d %s %f\", &students[i].roll_number, students[i].name, &students[i].marks);\r\n    }\r\n\r\n    qsort(students, N, sizeof(Student), compareStudents);\r\n\r\n    for (int i = 0; i < N; i++) {\r\n        printf(\"Roll Number: %d, Name: %s, Marks: %.2f\", students[i].roll_number, students[i].name, students[i].marks);\r\n    }\r\n\r\n    return 0;\r\n}","language":"c","time":"2025-04-24T06:52:48.597Z","isCorrect":false},{"code":"\r\n#include <stdio.h>\r\n#include <stdlib.h>\r\n#include <string.h>\r\n\r\ntypedef struct {\r\n    int roll_number;\r\n    char name[50];\r\n    float marks;\r\n} Student;\r\n\r\nint compareStudents(const void *a, const void *b) {\r\n    Student *studentA = (Student *)a;\r\n    Student *studentB = (Student *)b;\r\n    if (studentB->marks > studentA->marks) return 1;\r\n    else if (studentB->marks < studentA->marks) return -1;\r\n    else return 0;\r\n}\r\n\r\nint main() {\r\n    int N;\r\n    scanf(\"%d\", &N);\r\n\r\n    Student students[N];\r\n\r\n    for (int i = 0; i < N; i++) {\r\n        scanf(\"%d %s %f\", &students[i].roll_number, students[i].name, &students[i].marks);\r\n    }\r\n\r\n    qsort(students, N, sizeof(Student), compareStudents);\r\n\r\n    for (int i = 0; i < N; i++) {\r\n        printf(\"Roll Number: %d, Name: %s, Marks: %.2f\\n\", students[i].roll_number, students[i].name, students[i].marks);\r\n    }\r\n\r\n    return 0;\r\n}","language":"c","time":"2025-04-24T06:53:20.038Z","isCorrect":true}]